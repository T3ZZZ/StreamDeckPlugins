Collection of My Stream Deck Plugins

To install the plugins, simply run `npm ci` in the main folder (the one located in the root directory).

Once the installation is complete, move the folder to:
`C:/Users/%username%/AppData/Roaming/Elgato/StreamDeck/Plugins/`
